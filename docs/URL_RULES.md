# URL_RULES

Canonical rules:
- one object, one canonical URL
- canonical URL must resolve the object payload directly
- no duplicate canonical URLs for the same object

Rendering rules:
- page is the payload
- front of page shows action
- back of page shows evidence

Summary rules:
- summaries reveal patterns, not inventory
- avoid count-only summary blocks that do no decision work
